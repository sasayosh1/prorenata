; Ensure devDependencies install even if the user has global `omit=dev`
; or a production-like NODE_ENV. Required for Next.js (Tailwind/PostCSS/ESLint).
; `omit` must be one of: dev|optional|peer (or comma-separated). We override to omit only optional deps.
omit=optional
