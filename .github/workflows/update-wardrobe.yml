name: 👗 Update Sera Wardrobe

on:
  schedule:
    # 3月, 6月, 9月, 12月の1日 朝6:00 (JST) に実行 -> UTCで前日の21:00
    - cron: '0 21 28,31 2,5,8,11 *'
  
  # 手動でも実行できるようにする
  workflow_dispatch:

jobs:
  update-wardrobe:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          # obsidianのデータなど別リポジトリで管理している場合は、ここで必要なパスをcheckoutするか
          # スクリプト内でファイルパスをリポジトリ内に向ける必要があります。
          # 今回はプロレナタのリポジトリ内にコピーした「ダミー/連動先」に一旦保存するか、パスを考慮。
          fetch-depth: 0
          
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Dependencies
        run: |
          npm ci || npm install
          npm install @google/generative-ai

      - name: Generate New Wardrobe
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          HOME: ${{ github.workspace }}
        run: |
          # Create proxy directory to match script expectations if needed in CI environment
          mkdir -p "$HOME/Library/Mobile Documents/iCloud~md~obsidian/Documents/sasayoshi/00_System/00_UserProfile"
          touch "$HOME/Library/Mobile Documents/iCloud~md~obsidian/Documents/sasayoshi/00_System/00_UserProfile/08_セラのクローゼット(Sera_Wardrobe).md"
          
          node scripts/update-sera-wardrobe.cjs
          
          # If the master file is maintained somewhere else, you might need a sync step here.
          # For testing inside github actions, we'll copy it to a tracked location in this repo too.
          cp "$HOME/Library/Mobile Documents/iCloud~md~obsidian/Documents/sasayoshi/00_System/00_UserProfile/08_セラのクローゼット(Sera_Wardrobe).md" "./08_セラのクローゼット(Sera_Wardrobe)_mirror.md"

      - name: Commit and Push Changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add "08_セラのクローゼット(Sera_Wardrobe)_mirror.md"
          # Check if there are changes to commit
          if ! git diff-index --quiet HEAD; then
            git commit -m "chore(sera): Auto-update seasonal wardrobe 👗"
            git push
          else
            echo "No changes to commit."
          fi
