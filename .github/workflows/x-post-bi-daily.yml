name: Bi-Daily X Post Idea Generation

# on:
#   schedule:
#     # 21:00 UTC = 06:00 JST (Next day)
#     # Run every 2 days
#     - cron: '0 21 */2 * *'
#   workflow_dispatch:
# 
# jobs:
#   generate_x_posts:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: write
# 
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4
# 
#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'
# 
#       - name: Install dependencies
#         run: npm ci
# 
#       - name: Generate X Posts
#         env:
#           GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
#           SANITY_PROJECT_ID: ${{ secrets.SANITY_PROJECT_ID }}
#           SANITY_DATASET: ${{ secrets.SANITY_DATASET }}
#           SANITY_API_TOKEN: ${{ secrets.SANITY_API_TOKEN }}
#         run: npm run generate:x-posts
# 
#       - name: Commit and Push Changes
#         run: |
#           git config --global user.name "Sera Shirasaki (Bot)"
#           git config --global user.email "sera-bot@prorenata.jp"
#           
#           git add X投稿/
#           
#           if git diff --staged --quiet; then
#             echo "No changes to commit."
#             exit 0
#           fi
#           
#           git commit -m "feat(x): update bi-daily x post ideas [skip ci]"
#           git push
